---
title: "Week 3: Matrices, Lists, Data Frames & Data Import"
subtitle: "GPH-GU 2182: Statistical Programming in R · Fall 2026"
author: "Yang Feng, PhD"
institute: "New York University · School of Global Public Health"
date: "September 16, 2026"
format:
  revealjs:
    theme: default
    css: ../../styles.css
    slide-number: true
    preview-links: auto
    chalkboard: true
    transition: slide
    width: 1280
    height: 720
    margin: 0.06
    min-scale: 0.2
    max-scale: 2.0
    auto-stretch: false
---

## Welcome to Week 3!

::: {.columns}
::: {.column width="50%"}
### Today\'s Roadmap

1. **Matrices & 2D Indexing:** 2D homogeneous data, row/col operations.
2. **Lists:** Storing heterogeneous objects of different types & lengths.
3. **Data Frames & Tibbles:** The cornerstone of statistical data in R.
4. **Data Import & Export:** Loading real-world CSV & Excel files into R.
5. **AI Corner:** Debugging messy imports & type-parsing traps.
:::

::: {.column width="50%"}
### Today\'s Milestones

* **Homework 2:** Due tonight at 11:59 PM.
* **Homework 3:** Released today (Matrices, Lists, Data Frames & Import).
* 📝 **Quiz 1 (Weeks 1–2):** Administered during the last 30 minutes of class (4:10–4:40 PM). Closed-book, paper handout.
:::
:::

---

# Cycle 1: Matrices & 2D Indexing {background-color="#2c3e50"}


## Welcome & Check-In {background-color="#57068c"}

::: {.columns}
::: {.column width="58%"}
### 📋 Walk-In Check-In Protocol

1. **Find any open desk** and unpack your laptop.
2. **Scan the QR code** or enter today's 4-digit PIN on screen to check in.
3. **At 2:03 PM**, random pairs will be generated on the screen.
4. **Move to your assigned station** to sit with your partner *before* lecture starts!
:::

::: {.column width="42%"}
::: {.callout-note appearance="simple"}
### 👥 Today's Roles
* **🚀 Driver (Student A):** Types for Cycles 1 & 2
* **🧭 Navigator (Student B):** Types for Cycles 3 & 4
* *Swap keyboard & roles at mid-class break!*
:::
:::
:::


## What is a Matrix?

A **matrix** is a 2-dimensional collection of elements of the **same basic type** (homogeneous), arranged in rows and columns.

::: {.columns}
::: {.column width="50%"}
### Column-by-Column (Default)

```r
m1 <- matrix(1:6, nrow = 2, ncol = 3)
m1
```

```text
     [,1] [,2] [,3]
[1,]    1    3    5
[2,]    2    4    6
```
:::

::: {.column width="50%"}
### Row-by-Row (`byrow = TRUE`)

```r
m2 <- matrix(1:6, nrow = 2, ncol = 3, byrow = TRUE)
m2
```

```text
     [,1] [,2] [,3]
[1,]    1    2    3
[2,]    4    5    6
```
:::
:::

---

## Dimensions & Naming

```r
# Check matrix dimensions
dim(m2)    # returns c(number_of_rows, number_of_columns)
nrow(m2)   # 2 rows
ncol(m2)   # 3 columns

# Adding Row and Column Names
rownames(m2) <- c("Hospital_A", "Hospital_B")
colnames(m2) <- c("ICU_Beds", "Ventilators", "Nurses")
m2
```

```text
           ICU_Beds Ventilators Nurses
Hospital_A        1           2      3
Hospital_B        4           5      6
```

---

## 2D Matrix Subsetting: `[row, column]`

Always remember: **`[row, column]`** (think "RC Cola" or "Roman Catholic").

```r
# 1. Single element: row 1, col 2
m2[1, 2]                     # 2

# 2. Entire row: leave column blank!
m2[1, ]                      # ICU_Beds: 1, Ventilators: 2, Nurses: 3

# 3. Entire column: leave row blank!
m2[, "ICU_Beds"]             # Hospital_A: 1, Hospital_B: 4

# 4. Submatrix: rows 1:2, columns 2:3
m2[1:2, 2:3]

# 5. Conditional replacement
m2[m2 < 3] <- 0
```

---

## 👥 Paired Exercise 1: ICU Capacity Matrix (10 min)

::: {.columns}
::: {.column width="50%"}
### Task

In your `week-03-inclass.qmd` worksheet:
1. Construct a 3x3 matrix `icu_admissions` containing the numbers `c(12, 18, 5, 24, 30, 11, 8, 15, 4)` filled **row-by-row**.
2. Assign rownames: `c("Mon", "Tue", "Wed")` and colnames: `c("Site_1", "Site_2", "Site_3")`.
3. Compute the **total admissions per site** using `colSums(icu_admissions)`.
4. Extract all days where `Site_2` admissions exceeded 15.
:::

::: {.column width="50%"}
### Expected Output

```text
      Site_1 Site_2 Site_3
Mon       12     18      5
Tue       24     30     11
Wed        8     15      4

Site totals:
Site_1: 44, Site_2: 63, Site_3: 20
```

*Time Remaining: 10 minutes.*
:::
:::

---

# Cycle 2: Lists & Heterogeneous Data {background-color="#2c3e50"}

## Why Do We Need Lists?

Vectors and matrices are **homogeneous** (all elements must be the exact same type).

In public health and clinical research, real records contain **heterogeneous data**:
* Patient ID (character)
* Demographics (character, numeric)
* Blood pressure readings across multiple visits (numeric vector of length 5)
* Lab test results (logical or nested sub-records)

A **list** is a flexible container whose elements can be of **any type, length, or structure**!

---

## Creating & Inspecting Lists

```r
patient_record <- list(
  id = "PT-84920",
  age = 54,
  smoker = FALSE,
  sbp_history = c(138, 142, 130, 128, 135),
  metadata = list(clinic = "Bellevue", enrolled = "2026-01-15")
)

# Inspect structure
str(patient_record)
```

```text
List of 5
 $ id         : chr "PT-84920"
 $ age        : num 54
 $ smoker     : logi FALSE
 $ sbp_history: num [1:5] 138 142 130 128 135
 $ metadata   :List of 2
  ..$ clinic  : chr "Bellevue"
  ..$ enrolled: chr "2026-01-15"
```

---

## The Crucial Difference: `$` vs `[[ ]]` vs `[ ]`

::: {.columns}
::: {.column width="50%"}
### 1. `$` or `[[ ]]` (Extract Contents)

Extracts the **raw object inside** the list compartment:

```r
# Returns a NUMERIC VECTOR
patient_record$sbp_history
patient_record[["sbp_history"]]

# Now you can do math!
mean(patient_record$sbp_history) # 134.6
```
:::

::: {.column width="50%"}
### 2. `[ ]` (Sub-list / Slice)

Returns a **smaller list** containing that compartment:

```r
# Returns a LIST of length 1
sub_list <- patient_record[4]
typeof(sub_list) # "list"

# mean(sub_list) WILL FAIL!
# Lists cannot be averaged directly.
```
:::
:::

::: {.callout-tip}
*Think of a list as a train with cargo cars. `[4]` gives you the 4th car; `[[4]]` opens the car and gives you the cargo inside!*
:::

---

## 👥 Paired Exercise 2: Patient Trial List (10 min)

::: {.columns}
::: {.column width="50%"}
### Task

In `week-03-inclass.qmd`:
1. Build a list `study_subject` containing:
   * `name`: `"Jordan Lee"`
   * `arm`: `"Treatment_A"`
   * `viral_loads`: `c(45000, 12000, 850, 40)`
   * `adverse_events`: `0`
2. Extract the `viral_loads` vector and calculate the **percentage reduction** from baseline (visit 1 to visit 4).
3. Add a new element to the list named `completed` set to `TRUE`.
:::

::: {.column width="50%"}
### Expected Output

```text
Baseline: 45000
Final: 40
Reduction: 99.91%

$completed
[1] TRUE
```

*Time Remaining: 10 minutes.*
:::
:::

---

# Cycle 3: Data Frames & Tibbles {background-color="#2c3e50"}

## Data Frames: The Heart of Data Analysis

A **data frame** is a list of equal-length vectors. Each column is a variable; each row is an observation.

```r
# Creating a data frame
cohort <- data.frame(
  id = c("P01", "P02", "P03", "P04", "P05"),
  age = c(45, 62, 38, 71, 55),
  bmi = c(24.2, 31.0, 27.5, 29.8, 22.1),
  diabetes = c(FALSE, TRUE, FALSE, TRUE, FALSE),
  stringsAsFactors = FALSE
)

# Inspecting
nrow(cohort)       # 5 observations
ncol(cohort)       # 4 variables
summary(cohort)    # Summary statistics for each column
```

---

## Tibbles vs. Base Data Frames

A **tibble** (`tibble::tibble()`) is a modern data frame with refined behavior:

::: {.columns}
::: {.column width="50%"}
### Base `data.frame`
* Prints all rows and columns to console (can flood screen).
* May coerce strings or drop dimensions unpredictably (`df[, 1]` becomes a vector).
* Partial name matching (`df$d` might match `df$diabetes`).
:::

::: {.column width="50%"}
### `tibble` (`library(tibble)`)
* Clean printing: shows top 10 rows and variable types (`<chr>`, `<dbl>`, `<lgl>`).
* Strict subsetting: `tb[, 1]` always stays a tibble.
* Never coerces types or uses partial matching.
:::
:::

---

## Subsetting & Filtering Data Frames

```r
# 1. Accessing columns
cohort$bmi
cohort[["age"]]

# 2. Creating a new computed column
cohort$bmi_high <- cohort$bmi >= 30.0

# 3. Filtering rows by condition: df[condition, ]
seniors <- cohort[cohort$age >= 60, ]
seniors

# 4. Complex filtering: BMI > 25 AND non-diabetic
overweight_non_diab <- cohort[cohort$bmi > 25 & !cohort$diabetes, ]
```

---

## 👥 Paired Exercise 3: Cohort Filtering (10 min)

::: {.columns}
::: {.column width="50%"}
### Task

In `week-03-inclass.qmd`:
1. Use the provided `patients` data frame.
2. Add a new column `pulse_pressure` computed as `sbp - dbp`.
3. Filter all patients who meet **both** criteria:
   * Age >= 50
   * `pulse_pressure` > 45
4. Calculate the average `bmi` of this high-risk sub-cohort.
:::

::: {.column width="50%"}
### Hints

* Remember the comma after row condition: `patients[condition, ]`
* Use `mean(..., na.rm = TRUE)` if missing values exist.
:::
:::

---

# Cycle 4: Data Import & Export {background-color="#2c3e50"}

## Importing Real Data (CSV & Excel)

::: {.columns}
::: {.column width="50%"}
### 1. Reading CSV Files (`readr`)

```r
library(readr)

# read_csv() returns a clean tibble
nhanes_data <- read_csv(
  "data/nhanes_sample.csv",
  na = c("", "NA", "999", "Unknown")
)

head(nhanes_data)
```
:::

::: {.column width="50%"}
### 2. Reading Excel (`readxl`)

```r
library(readxl)

# Check available sheets
excel_sheets("data/clinical_trial.xlsx")

# Read specific sheet
trial_data <- read_excel(
  "data/clinical_trial.xlsx",
  sheet = "Baseline_Vitals",
  na = "NA"
)
```
:::
:::

---

## Exporting Cleaned Datasets

Once your data is cleaned and validated, save it back to disk:

```r
# Exporting to CSV
write_csv(cohort, "data/cleaned_cohort.csv")

# Saving R binary objects (.rds preserves exact data types!)
saveRDS(cohort, "data/cleaned_cohort.rds")
restored_cohort <- readRDS("data/cleaned_cohort.rds")
```

---

# AI Corner: The Silent Import Bug {background-color="#8e44ad"}

## The "Unknown" String Trap

::: {.columns}
::: {.column width="50%"}
### ❌ What AI Generated

```r
# Prompt: "Read this blood pressure dataset from CSV"
bp_df <- read.csv("bp_data.csv")
mean(bp_df$systolic)
```

**Output:** `[1] NA`

**Why?** In the CSV, row 42 contained `"Unknown"`. Base `read.csv` converted the **entire `systolic` column to character**!
:::

::: {.column width="50%"}
### ✅ The Verified Fix

Always specify explicit `na` strings upon import:

```r
library(readr)
bp_df <- read_csv(
  "bp_data.csv",
  na = c("", "NA", "Unknown", "Refused", "-99")
)

# Verify column types
spec(bp_df)
mean(bp_df$systolic, na.rm = TRUE)
```
:::
:::

---

# In-Class Quiz 1 (4:20–4:40 PM) {background-color="#c0392b"}

## 📝 Quiz 1 Instructions

1. **Laptops, tablets, and phones away.**
2. This is an **individual, closed-book, paper-based quiz** covering **Weeks 1–2**.
3. You have **20 minutes** (4:15–4:35 PM).
4. Hand your paper to the teaching team at 4:35 PM.
5. We will show the answer key on screen at 4:36 PM!

::: {.callout-note}
*Remember: Your lowest quiz score of the semester is automatically dropped!*
:::
