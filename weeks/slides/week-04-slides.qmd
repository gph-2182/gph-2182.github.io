---
title: "Week 4: Data Visualization with ggplot2"
subtitle: "GPH-GU 2182: Statistical Programming in R · Fall 2026"
author: "Yang Feng, PhD"
institute: "New York University · School of Global Public Health"
date: "September 23, 2026"
format:
  revealjs:
    theme: default
    css: ../../styles.css
    slide-number: true
    preview-links: auto
    chalkboard: true
    transition: slide
    width: 1280
    height: 720
    margin: 0.06
    min-scale: 0.2
    max-scale: 2.0
    auto-stretch: false
---

## Welcome to Week 4!

::: {.columns}
::: {.column width="50%"}
### Today\'s Roadmap

1. **The Grammar of Graphics:** Data, aesthetics, geometries.
2. **Visualizing Distributions:** Histograms, densities, boxplots.
3. **Small Multiples & Trends:** Faceting & smooth curves.
4. **Publication Polish:** Scales, viridis palettes, themes.
5. **AI Corner:** Spotting misleading axes and chartjunk.
:::

::: {.column width="50%"}
### Today\'s Milestones

* **Homework 3:** Due tonight at 11:59 PM.
* **Homework 4:** Released today (`ggplot2` visualization).
* **Looking Ahead:** Quiz 2 takes place next week in Week 5.
:::
:::

---

# Cycle 1: The Grammar of Graphics {background-color="#2c3e50"}


## Welcome & Check-In {background-color="#57068c"}

::: {.columns}
::: {.column width="58%"}
### 📋 Walk-In Check-In Protocol

1. **Find any open desk** and unpack your laptop.
2. **Scan the QR code** or enter today's 4-digit PIN on screen to check in.
3. **At 2:03 PM**, random pairs will be generated on the screen.
4. **Move to your assigned station** to sit with your partner *before* lecture starts!
:::

::: {.column width="42%"}
::: {.callout-note appearance="simple"}
### 👥 Today's Roles
* **🚀 Driver (Student A):** Types for Cycles 1 & 2
* **🧭 Navigator (Student B):** Types for Cycles 3 & 4
* *Swap keyboard & roles at mid-class break!*
:::
:::
:::


## Why `ggplot2`?

Created by Hadley Wickham based on Leland Wilkinson\'s *The Grammar of Graphics*.

* **Declarative System:** You describe *what* you want to see, not low-level drawing steps.
* **Layered Composition:** Stack data, aesthetics, geometries, facets, and themes using `+`.
* **Publication Standard:** Automatic legends, consistent scales, and reproducible output.

---

## Base R vs. `ggplot2`

::: {.columns}
::: {.column width="50%"}
### Base R (`plot()`)

* Fast for quick disposable sketches.
* Pen-on-paper model (cannot easily modify earlier layers).
* Hard to build multi-panel legends or faceting.
:::

::: {.column width="50%"}
### `ggplot2` (`library(ggplot2)`)

* Object-oriented: store plots in variables (`p <- ggplot(...)`).
* Effortless layering and faceting.
* Standard across biomedical and epidemiology research.
:::
:::

---

## The 3 Core Components of Every Plot

$$	ext{Plot} = 	ext{Data} + 	ext{Aesthetic Mappings} + 	ext{Geometric Layers}$$

```r
ggplot(data = <DATA>, mapping = aes(<MAPPINGS>)) +
  <GEOM_FUNCTION>()
```

1. **Data:** A `data.frame` or `tibble`.
2. **Aesthetics (`aes()`):** Visual channels ($x$, $y$, color, size, shape, alpha).
3. **Geometries (`geom_*()`):** Visual representation (points, lines, bars, boxes).

---

## Building a Scatterplot: Step-by-Step

```r
library(ggplot2)

# Step 1: Canvas (blank)
p <- ggplot(data = airquality, aes(x = Temp, y = Ozone))

# Step 2: Add Points
p + geom_point()

# Step 3: Map a 3rd Variable to Color
p + geom_point(aes(color = factor(Month)), size = 2.5, alpha = 0.8)
```

---

## Aesthetic Mapping vs. Setting Constants

::: {.columns}
::: {.column width="50%"}
### Mapping Inside `aes()`
Connects a visual channel to a **variable** in your data:

```r
# Color varies by Month
geom_point(aes(color = factor(Month)))
```
:::

::: {.column width="50%"}
### Setting Outside `aes()`
Sets a **fixed constant** for all observations:

```r
# All points are navy and size 3
geom_point(color = "navy", size = 3, alpha = 0.7)
```
:::
:::

::: {.callout-tip}
*Never put fixed values inside `aes()` (e.g. `aes(color = "blue")`), as R will treat `"blue"` as a single-level categorical factor!*
:::

---

## 👥 Paired Exercise 1: Air Quality Scatterplot (10 min)

::: {.columns}
::: {.column width="50%"}
### Task

In your `week-04-inclass.qmd` worksheet:
1. Use the built-in `airquality` dataset.
2. Create a scatterplot of Solar Radiation (`Solar.R`, $x$) vs Ozone (`Ozone`, $y$).
3. Map Wind Speed (`Wind`) to the **color** aesthetic.
4. Set point transparency `alpha = 0.7` and `size = 3` (outside `aes`).
5. Add a smooth trendline using `geom_smooth(method = "loess", color = "darkblue")`.
:::

::: {.column width="50%"}
### Key Observation

* High solar radiation paired with low wind velocity correlates strongly with peak ozone pollution episodes.
:::
:::

---

# Cycle 2: Visualizing Distributions {background-color="#2c3e50"}

## Histograms (`geom_histogram`)

Visualizing a single continuous variable:

```r
ggplot(airquality, aes(x = Temp)) +
  geom_histogram(binwidth = 5, fill = "#3498db", color = "white") +
  labs(x = "Temperature (°F)", y = "Count")
```

* Always test multiple `binwidth` values to avoid hiding peaks or creating artificial noise.

---

## Density Curves (`geom_density`)

Smooth probability density estimates:

```r
ggplot(airquality, aes(x = Temp, fill = factor(Month))) +
  geom_density(alpha = 0.4) +
  labs(x = "Temperature (°F)", y = "Density", fill = "Month")
```

* Use `alpha = 0.4` so overlapping sub-populations remain visible.

---

## Boxplots (`geom_boxplot`)

Comparing distributions across categorical groups:

```r
ggplot(airquality, aes(x = factor(Month), y = Ozone, fill = factor(Month))) +
  geom_boxplot(outlier.shape = 1, alpha = 0.7) +
  labs(x = "Month", y = "Ozone (ppb)")
```

* Displays 5-number summary (Min, Q1, Median, Q3, Max) + outlier flags.

---

## Boxplot + Jitter Overlay (`geom_jitter`)

Layering raw observations over boxplots reveals sample sizes and local clusters:

```r
ggplot(airquality, aes(x = factor(Month), y = Ozone, fill = factor(Month))) +
  geom_boxplot(outlier.shape = NA, alpha = 0.7) +
  geom_jitter(width = 0.2, alpha = 0.5, color = "black")
```

* Set `outlier.shape = NA` on the boxplot to prevent plotting outliers twice!

---

## Discrete Counts: `geom_bar` vs. `geom_col`

::: {.columns}
::: {.column width="50%"}
### `geom_bar()` (Counts Rows)
When R calculates frequency for you:

```r
# Data has raw individual rows
ggplot(airquality, aes(x = factor(Month))) +
  geom_bar(fill = "steelblue")
```
:::

::: {.column width="50%"}
### `geom_col()` (Pre-Summarized)
When data already has a numeric height column:

```r
summary_df <- data.frame(
  region = c("North", "South", "West"),
  admissions = c(120, 340, 210)
)
ggplot(summary_df, aes(x = region, y = admissions)) +
  geom_col(fill = "darkgreen")
```
:::
:::

---

## 👥 Paired Exercise 2: Comparing Ozone by Month (10 min)

::: {.columns}
::: {.column width="50%"}
### Task

In `week-04-inclass.qmd`:
1. Build a **violin plot** comparing `Ozone` across Months (`x = factor(Month)`, `y = Ozone`).
2. Fill violins by `factor(Month)` with `alpha = 0.5`.
3. Overlay a narrow boxplot (`width = 0.2`, `fill = "white"`) inside each violin.
:::

::: {.column width="50%"}
### Pro-Tip

Layer order matters! Put `geom_violin()` first, then `geom_boxplot()` on top so the boxplot sits cleanly inside the violin body.
:::
:::

---

# Cycle 3: Small Multiples & Trendlines {background-color="#2c3e50"}

## Faceting with `facet_wrap`

Split complex multi-group data into clean sub-panels:

```r
ggplot(airquality, aes(x = Temp, y = Ozone)) +
  geom_point(color = "navy", alpha = 0.7) +
  geom_smooth(method = "lm", se = FALSE, color = "red") +
  facet_wrap(~ Month, nrow = 1)
```

* `nrow = 1` aligns all panels in a single horizontal strip.

---

## Controlling Facet Scales

By default, all subplots share the exact same axis limits (`scales = "fixed"`).

```r
# Independent y-axis limits for each panel:
facet_wrap(~ Month, scales = "free_y")

# Completely independent axes:
facet_wrap(~ Month, scales = "free")
```

::: {.callout-note}
*Use fixed scales when comparing absolute quantities; use free scales when comparing internal distribution shapes.*
:::

---

## 👥 Paired Exercise 3: Multi-Panel Trends (10 min)

::: {.columns}
::: {.column width="50%"}
### Task

In `week-04-inclass.qmd`:
1. Plot `Temp` ($x$) vs `Ozone` ($y$) from `airquality`.
2. Add points with `alpha = 0.6`.
3. Add a linear regression line (`geom_smooth(method = "lm", color = "darkred")`).
4. Facet by `Month` into a single row (`nrow = 1`).
:::

::: {.column width="50%"}
### Discussion

Observe how the slope of the temperature-ozone relationship changes between early summer (May) and late summer (August)!
:::
:::

---

# Cycle 4: Polishing Publication Graphics {background-color="#2c3e50"}

## Informative Labels: `labs()`

Never leave raw column names on a presentation or report figure:

```r
p_pub <- ggplot(airquality, aes(x = Temp, y = Ozone, color = Wind)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", color = "black", linetype = "dashed", se = FALSE) +
  labs(
    title = "Air Pollution Dynamics at LaGuardia Airport",
    subtitle = "Daily measurements of Ozone, Temperature, and Wind (May - Sep 1973)",
    x = "Maximum Daily Temperature (°F)",
    y = "Mean Ozone (ppb)",
    color = "Wind Speed\n(mph)",
    caption = "Source: NYS Department of Environmental Conservation"
  )
```

---

## Colorblind-Safe Scales & Themes

::: {.columns}
::: {.column width="50%"}
### Viridis Palettes
```r
p_pub + 
  scale_color_viridis_c(option = "viridis")
```
* Perceptually uniform and legible in grayscale.
:::

::: {.column width="50%"}
### Clean Themes
```r
p_pub + 
  scale_color_viridis_c(option = "viridis") +
  theme_minimal(base_size = 13) +
  theme(legend.position = "bottom")
```
:::
:::

---

## Saving Figures: `ggsave()`

Save vector or high-resolution raster images for publications and reports:

```r
ggsave(
  filename = "figures/figure1_ozone.png",
  plot = p_pub,
  width = 8,
  height = 5,
  dpi = 300
)
```

---

# AI Corner: Misleading Visuals & Baselines {background-color="#8e44ad"}

## Spotting Truncated Baselines

::: {.columns}
::: {.column width="50%"}
### ❌ What AI Generated
```r
# AI Prompt: "Bar chart of mortality rate"
ggplot(df, aes(x = year, y = rate)) +
  geom_col() +
  coord_cartesian(ylim = c(48, 52))
```
* Truncating the $y$-axis baseline makes a 1% change look like a 500% surge!
:::

::: {.column width="50%"}
### ✅ The Verified Rule
* **Length/Area geoms (`geom_col`, `geom_bar`) MUST start at 0.**
* **For Overplotting:** Use `geom_jitter()`, `alpha < 0.5`, or 2D density contours.
* **Color Accessibility:** Always use colorblind-friendly scales (`viridis`).
:::
:::

---

# Wrap-Up & Next Steps

::: {.columns}
::: {.column width="50%"}
### Today\'s Key Takeaways

1. Grammar of Graphics: Data + `aes()` + `geom_*()`.
2. Right geoms for continuous vs. discrete health variables.
3. Multi-panel small multiples with `facet_wrap()`.
4. Professional polish with `labs()`, `theme_minimal()`, and `viridis`.
:::

::: {.column width="50%"}
### Looking Ahead to Week 5

* **Homework 4:** Released today (due next Wednesday, Sep 30).
* **Next Week:** Data Wrangling with `dplyr` & Pipes (filter, mutate, select, arrange).
* 📝 **Quiz 2 (Weeks 3–4):** In the last 30 minutes of Week 5!
:::
:::
